<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Haz tu voto</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
</head>
<body>

    <!-- Sección de Votación -->
    <section id="votacion" class="premios">
        <div class="container">
            <h2 class="text-center mb-5">Haz tu voto</h2>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <form action="/votacion" method="POST" class="p-4" style="border: 2px solid #FFD700; border-radius: 10px; background-color: #111111;">
                        @csrf
                        <!-- Nombre -->
                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre</label>
                            <input type="text" class="form-control bg-dark text-warning" id="nombre" name="nombre" placeholder="Tu nombre" required>
                        </div>
                        <!-- Correo -->
                        <div class="mb-3">
                            <label for="correo" class="form-label">Correo Electrónico</label>
                            <input type="email" class="form-control bg-dark text-warning" id="correo" name="correo" placeholder="tu@email.com" required>
                        </div>
                        <!-- Elección -->
                        <div class="mb-3">
                            <label for="eleccion" class="form-label">Elige tu favorito</label>
                            <select class="form-select bg-dark text-warning" id="eleccion" name="eleccion" required>
                                <option value="" disabled selected>Selecciona una opción</option>
                                <option value="opcion1">Opción 1</option>
                                <option value="opcion2">Opción 2</option>
                                <option value="opcion3">Opción 3</option>
                            </select>
                        </div>
                        <!-- Comentarios / Mensaje -->
                        <div class="mb-3">
                            <label for="mensaje" class="form-label">Mensaje (opcional)</label>
                            <textarea class="form-control bg-dark text-warning" id="mensaje" name="mensaje" rows="3" placeholder="Escribe un mensaje"></textarea>
                        </div>
                        <!-- Botones -->
                        <div class="text-center d-flex justify-content-between">
                            <button type="submit" class="btn btn-voto w-50">Enviar Voto</button>
                            <a href="/" class="btn btn-voto w-25">Volver</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
